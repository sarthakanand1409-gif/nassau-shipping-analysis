# Nassau Candy Distributor — Shipping Route Efficiency

Factory-to-customer shipping route efficiency analysis with a Streamlit dashboard.

## Quick Start

```bash
git clone <your-repo>
cd nassau-shipping-analysis
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt